<?

 class Theme {
   ### color set #1:
   var $brcolor1 = "#000000";   // border color
   var $bgcolor1 = "#C0C0C0";   // background color
   var $fgcolor1 = "#000000";   // table body color
   var $hlcolor1 = "#000000";   // high-light color
   var $categorycolor = "#202020";

   ### color set #2:
   var $bgcolor2 = "#EEEEEE";
   var $fgcolor2 = "#000000";
   var $hlcolor2 = "#000000";

   ### color set #3:
   var $bgcolor3 = "#D7D7D7";
   var $fgcolor3 = "#000000";
   var $hlcolor3 = "yellow";

   ######
   # Syntax.......: header($title);
   # Description..: a function to draw the page header.
   function header($title = '') {
    ?>
     <HTML>
     <HEAD>
      <TITLE><? include "config.inc"; echo $sitename; ?></TITLE>
      <META NAME="description" CONTENT="drop.org">
      <META NAME="keywords" CONTENT="drop, weblog, portal, community, news, article, announcements, stories, story, computer, science, space, hype, cult, geek, nerd, foo, bar">
     </HEAD>
     <STYLE type="text/css">
      <!--
       TD,P,UL,LI,DIV,FORM,EM,BLOCKQUOTE { font-size: 8pt; font-family: verdana,helvetica,arial; }
       BODY { margin: 10px; font-size: 9pt; font-family: verdana,helvetica,arial; }
       SMALL { font-size: 8pt; }
       BIG { font-size: 10pt; }
      -->
     </STYLE>
     <BODY TEXT="#000000" BGCOLOR="#C0C0C0" ALINK="#000000" LINK="#404040" VLINK="#404040" MARGINHEIGHT="10" MARGINWIDTH="10">
     <TABLE BORDER="0" CELLPADDING="0" CELLSPACING="0" ALIGN=CENTER>
     <TR><TD BGCOLOR="#000000"><IMG SRC="themes/UnConeD/images/null.gif" WIDTH="10"></TD><TD BGCOLOR="#C0C0C0"><IMG SRC="themes/UnConeD/images/null.gif" WIDTH="5"></TD>
     <TD BGCOLOR="#808080">
      <TABLE BORDER="0" CELLPADDING="0" CELLSPACING="9" BGCOLOR="#808080">
       <TR>
        <TD COLSPAN="2"><IMG SRC="/themes/UnConeD/images/logo.gif" ALT="drop.org logo"></TD>
       </TR>
       <TR>
        <TD COLSPAN="2" ALIGN="CENTER">
         <TABLE BORDER="0" WIDTH="100%" CELLSPACING="0" CELLPADDING="0" BGCOLOR="<? echo $this->brcolor1; ?>"><TR><TD ALIGN="CENTER"><TABLE BORDER="0" WIDTH="100%" CELLSPACING="1" CELLPADDING="4"><TR><TD ALIGN="CENTER" BGCOLOR="<? echo $this->bgcolor1; ?>"><BIG><A HREF="">home</A> | <A HREF="/faq.php">faq</A> | <A HREF="/search.php">search</A> | <A HREF="/submit.php">submit news</A> | <A HREF="/account.php">user account</A> | <A HREF="/webboard.php">webboard</A></BIG></TD></TR></TABLE></TD></TR></TABLE>
        </TD>
       </TR>
       <TR>
        <?
          dbconnect();
        ?>
       <TD VALIGN="top" WIDTH="560">
    <?
   }

   ######
   # Syntax.......: abstract(...);
   # Description..: a function to draw an abstract story box, that is the
   #                boxes displayed on the main page.
   function abstract2($editor, $informant, $timestamp, $subject, $abstract, $comments, $category, $department, $link) {
     $timestamp = date("l, F d, Y - h:i:s A", $timestamp);

     include "config.inc";
      ?>
      <TABLE BORDER="0" CELLPADDING="0" CELLSPACING="0" BGCOLOR="<? echo $this->brcolor1; ?>" WIDTH="100%">
      <TR><TD>
       <TABLE BORDER="0" CELLPADDING="4" CELLSPACING="1" WIDTH="100%">
        <TR BGCOLOR="<? echo $this->bgcolor1; ?>"><TD COLSPAN="2"><FONT COLOR="<? echo $this->fgcolor1; ?>"><B><? echo $subject; ?></B></FONT></TD></TR>
        <TR BGCOLOR="<? echo $this->bgcolor3; ?>">
         <TD>
         <?
          if ($informant) {
            print "<SMALL>Posted by <A HREF=\"account.php?op=userinfo&uname=$informant\">$informant</A> on $timestamp"; ?><? if ($department) echo "<BR>from the $department dept."; ?><? print "</SMALL></TD><TD ALIGN=\"center\"><B><A HREF=\"search.php?category=$category\"><FONT COLOR=\"$this->categorycolor\">$category</FONT></A></B>";
          }
          else {
            print "<SMALL>Posted by $anonymous on $timestamp"; ?><? if ($department) echo "<BR>from the $department dept."; ?><? print "</SMALL></TD><TD ALIGN=\"center\"><A HREF=\"search.php?category=$category\"><FONT COLOR=\"$this->categorycolor\">$category</FONT></A>";
          }
         ?>
        </TD>
       </TR>
       <TR BGCOLOR="<? echo $this->bgcolor2; ?>">
        <TD COLSPAN="2">
        <?
         if ($comments) {
           echo "<P>$abstract</P><P><FONT COLOR=\"$this->fgcolor3\">Editor's note by <A HREF=\"account.php?op=userinfo&uname=$editor\">$editor</A>:</FONT> $comments</P>";
         }
         else {
           echo $abstract;
         }
        ?>
        </TD>
       </TR>
      <TR BGCOLOR="<? echo $this->bgcolor3; ?>"><TD ALIGN="right" COLSPAN="2"><? echo $link ?></TD></TR>
     </TABLE></TD></TR></TABLE><BR>
    <?
   }

   ######
   # Syntax.......: article(...);
   # Description..: a function to dispay a complete article (without user
   #                comments).  It's what you get when you followed for
   #                instance one of read-more links on the main page.
   function article($editor, $informant, $timestamp, $subject, $department, $abstract, $comments, $article, $reply) {
     $timestamp = date("l, F d, Y - h:i:s A", $timestamp);

      include "config.inc";
       ?>
      <TABLE BORDER="0" CELLPADDING="0" CELLSPACING="0" BGCOLOR="<? echo $this->brcolor1; ?>" WIDTH="100%">
      <TR><TD>
       <TABLE BORDER="0" CELLPADDING="4" CELLSPACING="1" WIDTH="100%">
       <TR BGCOLOR="<? echo $this->bgcolor1; ?>"><TD><FONT COLOR="<? echo $this->fgcolor1; ?>"><B><? echo $subject; ?></B></FONT></TD></TR>
       <TR BGCOLOR="<? echo $this->bgcolor3; ?>">
        <TD>
         <?
          if ($informant) {
            print "<FONT SIZE=\"-1\">Posted by <A HREF=\"account.php?op=userinfo&uname=$informant\">$informant</A> on $timestamp"; ?><? if ($department) echo "<BR>from the $department dept."; ?><? print "</FONT>";
          }
          else {
            print "<FONT SIZE=\"-1\">Posted by $anonymous on $timestamp"; ?><? if ($department) echo "<BR>from the $department dept."; ?><? print "</FONT>";
          }
         ?>
        </TD>
       </TR>
       <TR BGCOLOR="<? echo $this->bgcolor2; ?>">
        <TD>
         <?
          if ($abstract) echo "<P>$abstract<P>";
          if ($comments) echo "<P><FONT COLOR=\"$this->bgcolor1\">Editor's note by <A HREF=\"account.php?op=userinfo&uname=$editor\">$editor</A>:</FONT> $comments</P>";
          if ($article) echo "<P>$article</P>";
         ?>
        </TD>
       </TR>
      <TR BGCOLOR="<? echo $this->bgcolor3; ?>"><TD ALIGN="right"><? echo "$reply"; ?></TD></TR>
     </TABLE></TD></TR></TABLE><BR>
    <?
   }

   ######
   # Syntax.......: comment(...);
   # Description..: this function is used to theme user comments.
   function comment($poster, $subject, $tid, $date, $url, $email, $score, $reason, $comment, $link, $thread = "") {
     include "config.inc";

     if (!eregi("[a-z0-9]",$poster)) $poster = $anonymous;
     if (!eregi("[a-z0-9]",$subject)) $subject = "[no subject]";
     echo "<A NAME=\"$tid\">";

     ?>
      <TABLE BORDER="0" CELLPADDING="0" CELLSPACING="0" BGCOLOR="<? echo $this->brcolor1; ?>" WIDTH="100%">
      <TR><TD>
     <?

     ### Create comment header:
     echo "<TABLE BORDER=\"0\" CELLPADDING=\"4\" CELLSPACING=\"1\" WIDTH=\"100%\">";
     echo " <TR BGCOLOR=\"$this->bgcolor3\">";
     echo "  <TD>";
     echo "   <TABLE BORDER=\"0\" CELLPADDING=\"0\" CELLSPACING=\"1\"WIDTH=\"100%\">";

     ### Subject:
     echo "    <TR>";
     echo "     <TD ALIGN=\"right\" VALIGN=\"top\" WIDTH=\"5%\"><FONT COLOR=\"$this->hlcolor1\"><B>Subject:</B></FONT></TD><TD WIDTH=\"80%\">";
     echo "      <B><FONT COLOR=\"$this->fgcolor1\">$subject</FONT></B>";
     echo "<BR>[Score: $score";
     if (isset($reason)) echo ", $comments_meta_reasons[$reason]";
     echo "]</FONT>";
     echo "     </TD>";

     ### Moderation:
     echo "     <TD ALIGN=\"right\" ROWSPAN=\"3\" VALIGN=\"middle\" WIDTH=\"15%\">";
     echo "      <SELECT NAME=\"meta:$tid\">";
     echo "         <OPTION VALUE=\"-1\">Moderate</OPTION>\n";
     for ($i = 0; $i < sizeof($comments_meta_reasons); $i++) {
       echo "       <OPTION VALUE=\"$i\">$comments_meta_reasons[$i]</OPTION>\n";
     }
     echo "      </SELECT>";
     echo "     </TD>";
     echo "    </TR>";

     ### Author:
     echo "    <TR>";
     echo "     <TD ALIGN=\"right\" VALIGN=\"top\">Author:</TD><TD><B>$poster</B> ";
     if ($poster != $anonymous) {
       ### Display extra information line:
       $info .= "<A HREF=\"account.php?op=userinfo&uname=$poster\">user info</A>";
       if ($email) $info .= " | <A HREF=\"mailto:$email\">$email</A>";
       if (eregi("http://",$url)) $info .= " | <A HREF=\"$url\" TARGET=\"_new\">$url</A>";
       echo "<BR><FONT SIZE=\"2\">[ $info ]</FONT>";
     }
     echo "     </TD>";
     echo "    </TR>";

     ### Date
     echo "    <TR><TD ALIGN=\"right\">Date:</TD><TD>". formatTimestamp($date) ."</TD></TR>";

     echo "   </TABLE>";
     echo "  </TD>";
     echo " </TR>";

     ### Print body of comment:
     if ($comment) echo " <TR><TD BGCOLOR=\"$this->bgcolor2\">$comment</TD></TR>";

     ### Print thread (if any):
     if ($thread) echo " <TR><TD BGCOLOR=\"$this->bgcolor3\">$thread</TD></TR>";

     ### Print bottom link(s):
     echo " <TR><TD ALIGN=\"right\" BGCOLOR=\"$this->bgcolor3\">[ $link ]</TD></TR>";
     echo " </TABLE>";
     ?></TD></TR></TABLE><?
   }

   ######
   # Syntax.......: preview(...);
   # Description..: this function is used to preview a story and is used at
   #                different parts of the homepage: when a visitors sumbits
   #                news, when an editor wants to post news, when people
   #                check the entries in the sumbission queue, etc.
   function preview($editor, $informant, $timestamp, $subject, $department, $abstract, $comments, $article) {
     include "config.inc";
     ?>
      <TABLE BORDER="0" CELLPADDING="0" CELLSPACING="0" BGCOLOR="<? echo $this->brcolor1; ?>" WIDTH="100%">
      <TR><TD>
      <TABLE BORDER="0" CELLPADDING="4" CELLSPACING="1" WIDTH="100%">
       <TR BGCOLOR="<? echo $this->bgcolor1; ?>"><TD><FONT COLOR="<? echo $this->fgcolor1; ?>"><B><? echo $subject; ?></B></FONT></TD></TR>
       <TR BGCOLOR="<? echo $this->bgcolor3; ?>">
        <TD>
         <?
          if ($informant) {
            print "<SMALL>Posted by <A HREF=\"account.php?op=userinfo&uname=$informant\">$informant</A> on $timestamp"; ?><? if ($department) echo "<BR>from the $department dept."; ?><? print "</SMALL>";
          }
          else {
            print "<SMALLPosted by $anonymous on $timestamp"; ?><? if ($department) echo "<BR>from the $department dept."; ?><? print "</SMALL>";
          }
         ?>
        </TD>
       </TR>
       <TR BGCOLOR="<? echo $this->bgcolor2; ?>">
        <TD>
         <?
          if ($abstract) echo "<P>$abstract<P>";
          if ($comments) echo "<P><FONT COLOR=\"$this->fgcolor1\">Editor's note by <A HREF=\"account.php?op=userinfo&uname=$editor\">$editor</A>:</FONT> $comments</P>";
          if ($article) echo "<P>$article</P>";
        ?>
       </TD>
      </TR>
      <TR BGCOLOR="<? echo $this->bgcolor3; ?>"><TD ALIGN="right">&nbsp;</TD></TR>
     </TABLE></TD></TR></TABLE><BR>
    <?
   }

   ######
   # Syntax.......: box($title, $body);
   # Description..: a function to draw a box/block.
   function box($subject, $content) {
     include "config.inc";
     ?>
      <TABLE BORDER="0" CELLPADDING="0" CELLSPACING="0" BGCOLOR="<? echo $this->brcolor1; ?>" WIDTH="100%">
      <TR><TD>
     <?

     print "<TABLE BORDER=\"0\" CELLPADDING=\"3\" CELLSPACING=\"1\" WIDTH=\"100%\">";
     print " <TR><TD ALIGN=\"center\" BGCOLOR=\"$this->bgcolor1\"><FONT COLOR=\"$this->fgcolor1\"><B>$subject</B></FONT></TD></TR>";
     print " <TR><TD BGCOLOR=\"$this->bgcolor2\">$content</TD></TR>";
     print "</TABLE></TD></TR></TABLE><BR>";


   }

   ######
   # Syntax.......: footer();
   # Description..: a function to draw the page footer.
   function footer() {
    ?>
       </TD>
       <TD VALIGN="top" WIDTH="150">
         <?
         global $PHP_SELF;

         if (strstr($PHP_SELF, "index.php")) {
           global $user;

           ### Display login box:
           displayAccount($this);

           ### Display voting poll:
           displayPoll($this);

           ### Display old headlines:
           displayOldHeadlines($this);
         }
         elseif (strstr($PHP_SELF, "account.php")) {
           ### Display account settings:
           displayAccountSettings($this);
         }
         elseif (strstr($PHP_SELF, "article.php")) {
           global $sid;

           ### Display related links:
           displayRelatedLinks($this, $sid);

           ### Display new headlines:
           displayNewHeadlines($this);
         }
         else {
           ### Display new headlines:
           displayNewHeadlines($this);
         }
         ### Display admin blocks:
         displayAdminblock($this);

         ### Display referring sites:
         displayReferrals($this);
        ?>
       </TD>
      </TR>
      <TR>
       <TD COLSPAN="2">
        <TABLE BORDER="0" WIDTH="100%" CELLSPACING="0" CELLPADDING="0" BGCOLOR="<? echo $this->brcolor1; ?>"><TR><TD ALIGN="CENTER"><TABLE BORDER="0" WIDTH="100%" CELLSPACING="1" CELLPADDING="4"><TR><TD ALIGN="CENTER" BGCOLOR="<? echo $this->bgcolor1; ?>"><BIG><A HREF="">home</A> | <A HREF="/faq.php">faq</A> | <A HREF="/search.php">search</A> | <A HREF="/submit.php">submit news</A> | <A HREF="/account.php">user account</A> | <A HREF="/webboard.php">webboard</A></BIG></TD></TR></TABLE></TD></TR></TABLE>
       </TD>
      </TR>
     </TABLE>
    </TD><TD BGCOLOR="#C0C0C0"><IMG SRC="themes/UnConeD/images/null.gif" WIDTH="5"></TD><TD BGCOLOR="#000000"><IMG SRC="themes/UnConeD/images/null.gif" WIDTH="10"></TD></TR>
    </TABLE>
    </BODY>
    </HTML>
    <?
   }
 }

?>
